name: Build Invenio Package

on:
  push:
    branches: [ main, master ]  # Ejecuta el flujo al subir cambios a main o master
  pull_request:
    branches: [ main, master ]

jobs:
  build-and-package:
    runs-on: ubuntu-22.04

    steps:
      # 1. Descargar el código del repositorio
      - name: Checkout code
        uses: actions/checkout@v4

      # 2. Configurar Python 3.9 (Sin caché estricto para evitar fallos de lectura)
      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: '3.9'

      # 3. Configurar Node.js (Requerido para compilar la interfaz de Invenio)
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      # 4. Instalar Pipenv y dependencias de Python (Generando el Lockfile en la nube)
      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pipenv
          # Forzamos la creación del archivo de bloqueo para que la instalación sea 100% estable
          pipenv lock
          pipenv install --deploy --dev

      # 5. Inyectar parches de compatibilidad críticos
      - name: Inject critical patches
        run: |
          pipenv run pip install "wtforms>=3.0.0" sentry-sdk

      # 6. Compilar los Assets Visuales (Estilos, JS, React)
      - name: Build Web Assets
        env:
          INVENIO_INSTANCE_PATH: ${{ github.workspace }}/instance
        run: |
          pipenv run invenio collect -v
          pipenv run invenio assets build

      # 7. Empaquetar el proyecto en un archivo ZIP listo para descargar
      - name: Create Production Artifact
        run: |
          sudo apt-get install -y zip
          zip -r invenio-build.zip . -x "*.git*" "node_modules/*" "__pycache__/*"

      # 8. Guardar el paquete en GitHub Actions por 7 días
      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: invenio-ready-package
          path: invenio-build.zip
          retention-days: 7
