name: Descargar Modelos Bark y Tortoise

on:
  workflow_dispatch:

jobs:
  descargar_pesados:
    runs-on: ubuntu-latest
    steps:
      - name: Liberar espacio en el servidor (Obligatorio)
        run: |
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc
          sudo apt-get clean

      - name: Instalar gestor de descargas de Hugging Face
        run: pip install huggingface_hub

      - name: Descargar los modelos a una carpeta local
        run: |
          # Creamos la carpeta donde se guardarán los modelos
          mkdir -p ./hf_cache
          
          # Le decimos a Hugging Face que use esta carpeta como destino
          export HF_HOME=$(pwd)/hf_cache
          
          # Descargamos los modelos base de Bark
          echo "Descargando Bark..."
          huggingface-cli download suno/bark
          
          # Descargamos los modelos base de Tortoise
          echo "Descargando Tortoise..."
          huggingface-cli download jbetker/tortoise-tts-v2

      - name: Comprimir los modelos
        run: |
          # Usamos tar.gz porque mantiene los permisos de los archivos intactos para Linux
          tar -czf modelos-tts-cache.tar.gz ./hf_cache

      - name: Subir como artefacto para descargar
        uses: actions/upload-artifact@v4
        with:
          name: Modelos-Voz-Pesados
          path: modelos-tts-cache.tar.gz
          retention-days: 3
